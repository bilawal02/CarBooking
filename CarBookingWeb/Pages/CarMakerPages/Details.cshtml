@page
@model CarBookingWeb.Pages.CarMakerPages.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CarMaker</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CarMaker.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CarMaker.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.CarMaker.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
